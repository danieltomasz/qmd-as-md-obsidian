# macOS
.DS_Store

# Intellij
*.iml
.idea

# npm
node_modules

# build
# NOTE: styles.css is a hand-written source file, not a build artefact —
# it must stay tracked so it ships with the plugin.
main.js
*.js.map
data.json
release.zip
release-local/
_archive/
.claude/
.vscode/